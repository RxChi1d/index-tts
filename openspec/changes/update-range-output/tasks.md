## 1. 實作
- [x] 1.1 在 `concat_tts.py` 新增範圍設定（1-based、含起訖），並以範圍選取檔案取代固定前 20 個。
- [x] 1.2 若範圍超出可用檔案數或起訖不合法，腳本需直接報錯並停止。
- [x] 1.3 依範圍自動命名輸出檔案（固定 3 位數，例如 `021-040.wav`），確保輸出落在 `outputs/`。
- [x] 1.4 調整檔案排序為「前綴數字優先，無前綴置後」，避免未補零造成順序錯誤。
- [x] 1.5 在執行 TTS 前列印選取檔案清單，順序與實際串接一致。
- [x] 1.6 明確定義 `RANGE_END` 與 `MAX_FILES` 的優先規則（設定 `RANGE_END` 時忽略 `MAX_FILES`；僅設定 `RANGE_START` 時以 `MAX_FILES` 補足範圍）。

## 2. 驗證
- [ ] 2.1 建立 40 個測試檔案，驗證選取 21–40 時輸出命名為 `021-040.wav` 且內容正確。
- [ ] 2.2 測試範圍超出可用檔案數時會回報錯誤。
