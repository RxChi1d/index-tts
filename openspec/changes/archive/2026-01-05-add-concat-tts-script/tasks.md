## 1. 實作
- [x] 1.1 新增專案根目錄腳本（暫定 `concat_tts.py`），以程式內變數設定輸入資料夾、參考音訊與 CLI 參數，並呼叫 `indextts/cli_v2.py` 且不攔截 stdout/stderr。
- [x] 1.2 實作文字處理流程：依檔名排序取前 20 個 `.txt`、移除檔案內/檔案間換行、合併為單行、繁轉簡後輸入 CLI。
- [x] 1.4 針對 Windows 命令列長度限制，改以 `.txt` 檔輸入文字並改用 `--text-file` 呼叫 CLI。
- [ ] 1.3 新增最小化測試（僅覆蓋檔案排序/取前 20/移除換行/合併邏輯），不執行實際 TTS。

## 2. 驗證
- [ ] 2.1 以臨時資料夾建立測試用 `.txt`，確認合併與繁轉簡結果正確。
- [ ] 2.2 手動執行腳本確認 CLI 輸出完整顯示。
